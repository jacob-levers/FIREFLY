[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
markers =
    slow: integration or real-data regression test that may take substantially longer
