sptpalm-env/
__pycache__/
*.pyc
.DS_Store
dist/
build/
*.egg-info/
*.spec.bak

# Calibration / backend-comparison tooling — kept local, not shipped with
# the program.  These scripts compare the Torch backend against trackpy /
# palmTRACER and are developer-only.
tools/calibrate_torch_vs_trackpy.py
tools/compare_overlay_plots.py
tools/compare_to_palmtracer.py
# More dev-only tooling — kept local, not on the repo / not shipped.
# gen_preview_panels regenerates the Preferences preview PNGs; calibrate_atrous
# derived the live _ATROUS_K_SIGMA=2.0 (the constant stays in the code);
# StyleGuide is an unused component gallery that was bloating the bundle.
tools/gen_preview_panels.py
scripts/calibrate_atrous.py
firefly/ui/qml/components/StyleGuide.qml

# Local agent / session data
.claude/

# Benchmark datasets + outputs — large external data / developer tooling, kept
# local (not shipped with the program).  The EPFL/SPT challenge stacks are
# hundreds of MB; benchmark_report holds generated figures + analysis scripts.
_epfl/
challenge/
challenge_data/
benchmark_report/
