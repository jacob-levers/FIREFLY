# Source-install bootstrap.
#
# Runtime dependencies live only in pyproject.toml.  Keeping a second hand-made
# list here let a ``git pull`` leave an otherwise healthy source virtualenv
# without a newly added dependency.  Editable installation keeps source changes
# visible while resolving the same dependency metadata as a normal wheel.
-e .
